'use strict';
module.exports = ageDiff();
function ageDiff () {
  return{
    meanMomWhenChildAge: x
  };
  function age(a,b){
    return a - b;
  }
  function sum(a,b){
    return a + b;
  }
  function x(arrayOfPeople){
    var withMoms = arrayOfPeople.filter(function (person){
                      if(person.mother){
                        return person;
                      }
                    });
    var byName = {};
    withMoms.forEach(function(person){
      byName[person.name] = person;
    });
    // console.log('withMoms: ', withMoms);
    // console.log('byName: ', byName);
    var momName = withMoms[1].mother;
    console.log(withMoms[1].name,'s mom is:',momName.trim());
    // return withMoms.map(function (person){
    //             return age(person.born, byName[person.mother][born]);
    //           }).reduce(sum(pev,curr))/ withMoms.length;
  }
}