'use strict';

module.exports = every;

function every (array, fn) {
  var result = true;
  for ( var i = 0; i < array.length && result; i++){
    if(!fn(array[i]) ){
      result = false;
    }
  }
  return result;
}