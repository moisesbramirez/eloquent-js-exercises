'use strict';
module.exports = flattening();

function flattening () {
  return {
    flattenArrs:x
  };
  function x(arrOfArrs){
    return arrOfArrs.reduce(function(arr, curr){
      return arr.concat(curr);
    });
  }
}