'use strict';

module.exports = some;

function some (array, fn) {
  var result = false;
  for ( var i = 0; i < array.length && !result; i++){
    if(fn(array[i]) ){
      result = true;
    }
  }
  return result;
}